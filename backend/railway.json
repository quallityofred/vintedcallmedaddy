{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "poetry install --only main --no-root"
  },
  "deploy": {
    "startCommand": "poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
